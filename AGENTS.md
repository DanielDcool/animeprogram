# AI 协作入口

本仓库由用户与 Codex、Claude 共同维护。开始较重要的工作前，按以下顺序建立上下文：

1. 阅读 `docs/DEVELOPMENT.md`：当前实现、关键决策、已知问题、路线图和完成标准。
2. 阅读 `README.md`：用户实际安装与使用流程。
3. 执行 `git status --short` 和 `git log -5 --oneline`，确认当前分支与未提交改动。
4. 再检查与任务直接相关的代码和测试；代码与测试是行为事实，若与文档冲突，先指出冲突并在本次改动中校正文档。

安装或首次本地运行时，先按 `README.md` 与 `docs/AI-SETUP.md` 的顺序执行；后者定义了凭证、个人媒体和本地数据的安全边界。

## 文档边界

- `docs/DEVELOPMENT.md` 是项目状态和未来设计的**唯一事实源**，每次功能迭代后同步更新。
- `README.md` 只放用户需要的安装、启动和功能用法，不堆实现历史。
- `docs/superpowers/specs/` 与 `docs/superpowers/plans/` 是已确认设计和历史实现计划；不要把旧计划里的未勾选项直接当成当前待办。
- 本文件与 `CLAUDE.md` 只做入口和协作约束，不复制完整项目状态，避免多份文档漂移。

## 工作原则

- 这是 Daniel 自用的本地日语学习播放器；优先解决真实学习摩擦，保持简单，不做未被要求的通用平台能力。
- 修改前明确成功标准；有多种解释且会改变范围时先询问，不静默替用户决定。
- 只改当前任务所需内容，保留用户及其他 AI 的未提交改动。
- 纯逻辑、状态机、解析器、文件选择和路由按 TDD 推进；完成前运行 `npm test`。
- 前端请求统一放在 `web/src/api.ts`；学习模式状态放在 `web/src/player/learningMode.ts`；颜色复用 `web/src/index.css` 的 `:root` 变量。
- 涉及 UI 的改动除自动测试外，还要在真实浏览器里验证核心链路。
- 涉及视频或音频的自动化验证必须全程无声：启动播放前先明确确认媒体已静音；如果当前工具无法确认或设置静音，就不得播放真实媒体，必须改用暂停态、静音样片、纯状态测试或其他无声方法。即使只播放几秒也不例外；若验证改变了观看进度，结束前恢复原值。
- 不在文档、提交、终端输出或测试夹具中写入 API key、Cookie、Token 或其他凭证。只能记录“已配置/未配置”。
- 不假设分支名。只有用户明确要求提交时，才把一个完整功能做成一个提交，并使用简洁的中文提交信息；推送同样需要明确授权。

## 交接收尾

完成较重要的工作时：

1. 更新 `docs/DEVELOPMENT.md` 中受影响的现状、决策、问题或路线图。
2. 如用户用法发生变化，再更新 `README.md`。
3. 报告实际执行过的验证和仍未闭环事项；不要用“应该通过”代替证据。
